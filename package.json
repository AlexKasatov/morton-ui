{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean",
    "reinstall": "pnpm -r exec rm -rf node_modules && pnpm install",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "demo": "pnpm run --filter demo dev"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.5.11",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  },
  "name": "morton-ui"
}
